{
  "name": "bezorgfilters-ff-addon",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox add-on that adds price-range filters (delivery fee, minimum order) to thuisbezorgd.nl.",
  "devDependencies": {
    "jsdom": "^30.1.1"
  },
  "scripts": {
    "test": "node test/filters.test.js && node test/content.dom.test.js && node test/manifest.test.js"
  },
  "engines": {
    "node": ">=24"
  }
}
